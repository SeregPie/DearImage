import DearImage from '../DearImage';

import './';

describe('DearImage.prototype.rotateCounterClockwise', () => {
	it.skip('should work', () => {
		// todo: get image
		let image;
		let otherImage = image.rotateCounterClockwise();
		// todo: check snapshot
		expect(otherImage);
	});
});
