import DearImage from '../DearImage';

import './';

describe('DearImage.prototype.rotateClockwise', () => {
	it.skip('should work', () => {
		// todo: get image
		let image;
		let otherImage = image.rotateClockwise();
		// todo: check snapshot
		expect(otherImage);
	});
});
