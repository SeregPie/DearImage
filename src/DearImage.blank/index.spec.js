import DearImage from '../DearImage';

import './';

describe('DearImage.blank', () => {
	it.skip('should work', () => {
		{
			// todo: which args
			let image = DeatImage.blank(a, b);
			// check snapshot
		}
		{
			let image = DeatImage.blank();
			// check snapshot
		}
	});
});
